<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Tenant - LeaseHub Admin</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/admin/dashboard">🏢 LeaseHub Admin</a>
    </nav>
    <div class="container mt-4">
        <a href="/admin/tenants" class="btn btn-link mb-3"><i class="fas fa-arrow-left"></i> Back to Tenants</a>
        <div class="card">
            <div class="card-header">
                <h4 class="mb-0"><i class="fas fa-user"></i> {{tenant.firstname}} {{tenant.lastname}}</h4>
            </div>
            <div class="card-body">
                <p><strong>Email:</strong> {{tenant.email}}</p>
                <p><strong>Phone:</strong> {{tenant.phone}}</p>
                <p><strong>Status:</strong> {{tenant.status}}</p>
                <p><strong>Property:</strong>
                    {{#if tenant.propertyId}}
                        {{tenant.propertyId.propertyname}}
                    {{else}}
                        <em>No property assigned</em>
                    {{/if}}
                </p>
            </div>
        </div>
    </div>
</body>
</html>
