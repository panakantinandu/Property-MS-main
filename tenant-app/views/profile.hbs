<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - Tenant Portal</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: #f7f9fc;
            min-height: 100vh;
            padding: 24px 0;
            color: #2d3748;
        }
        .profile-container {
            max-width: 960px;
            margin: 0 auto;
            padding: 0 16px;
        }
        .profile-card {
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(42, 51, 83, 0.08);
            overflow: hidden;
            margin-bottom: 20px;
        }
        .profile-header {
            background: linear-gradient(135deg, #5b6df8 0%, #7c5bff 100%);
            color: white;
            padding: 32px;
            text-align: center;
        }
        .profile-avatar {
            width: 110px;
            height: 110px;
            border-radius: 30px;
            background: rgba(255,255,255,0.15);
            margin: 0 auto 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 46px;
            color: #fff;
            border: 1px solid rgba(255,255,255,0.25);
        }
        .profile-body {
            padding: 28px;
        }
        .nav-tabs {
            border: none;
            margin-bottom: 24px;
            gap: 8px;
        }
        .nav-tabs .nav-link {
            color: #4a5568;
            border: 1px solid #e2e8f0;
            padding: 10px 18px;
            font-weight: 600;
            border-radius: 10px;
            background: #f8fafc;
        }
        .nav-tabs .nav-link.active {
            color: #1a202c;
            border-color: #5b6df8;
            background: #e9ecff;
            box-shadow: 0 6px 18px rgba(91, 109, 248, 0.2);
        }
        .info-section {
            margin-bottom: 25px;
        }
        .info-section h5 {
            color: #667eea;
            font-weight: 600;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e9ecef;
        }
        .info-row {
            display: flex;
            padding: 12px 0;
            border-bottom: 1px solid #f8f9fa;
        }
        .info-label {
            font-weight: 600;
            color: #495057;
            min-width: 180px;
        }
        .info-value {
            color: #6c757d;
            flex: 1;
        }
        .btn-edit {
            background: linear-gradient(135deg, #5b6df8 0%, #7c5bff 100%);
            border: none;
            color: white;
            padding: 11px 30px;
            border-radius: 10px;
            font-weight: 600;
            box-shadow: 0 8px 24px rgba(91, 109, 248, 0.35);
        }
        .btn-edit:hover {
            transform: translateY(-2px);
            color: white;
        }
        .btn-send-otp {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
            padding: 8px 20px;
            border-radius: 6px;
            font-size: 13px;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }
        .btn-send-otp:hover {
            transform: translateY(-1px);
            color: white;
        }
        .btn-send-otp:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 8px;
        }
        .form-control {
            border-radius: 8px;
            border: 1px solid #dee2e6;
            padding: 10px 15px;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .back-link {
            color: white;
            text-decoration: none;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 20px;
        }
        .back-link:hover {
            color: white;
            text-decoration: underline;
        }
        .status-badge {
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
        }
        .status-active {
            background: #d4edda;
            color: #155724;
        }
        .password-section {
            background: #f8fafc;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
            border: 1px dashed #e2e8f0;
        }
        .alert {
            border-radius: 8px;
        }
        .password-requirements {
            background: #e9ecff;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            border-left: 4px solid #667eea;
        }
        .password-requirements h6 {
            color: #667eea;
            font-weight: 600;
            margin-bottom: 10px;
        }
        .requirement-item {
            font-size: 13px;
            color: #495057;
            margin: 5px 0;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .requirement-item.met {
        </script>
    </body>
    </html>
                                    Leave these fields blank if you don't want to change your password.
                                </p>

                                <!-- Password validation error alert -->
                                <div id="passwordError" class="alert alert-danger d-none" role="alert" style="font-size: 14px;">
                                    <i class="fas fa-exclamation-circle"></i> <span id="passwordErrorMsg"></span>
                                </div>

                                <!-- Password requirements display -->
                                <div class="password-requirements d-none" id="passwordRequirements">
                                    <h6><i class="fas fa-check-circle"></i> Password Requirements:</h6>
                                    <div class="requirement-item unmet" id="req-length">
                                        <i class="fas fa-times"></i> At least 8 characters
                                    </div>
                                    <div class="requirement-item unmet" id="req-uppercase">
                                        <i class="fas fa-times"></i> At least 1 uppercase letter (A-Z)
                                    </div>
                                    <div class="requirement-item unmet" id="req-lowercase">
                                        <i class="fas fa-times"></i> At least 1 lowercase letter (a-z)
                                    </div>
                                    <div class="requirement-item unmet" id="req-number">
                                        <i class="fas fa-times"></i> At least 1 number (0-9)
                                    </div>
                                    <div class="requirement-item unmet" id="req-special">
                                        <i class="fas fa-times"></i> At least 1 special character (!@#$%^&*)
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-4">
                                        <div class="form-group">
                                            <label class="form-label">Current Password</label>
                                            <input type="password" class="form-control" name="currentPassword" id="currentPassword" placeholder="Enter current password">
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="form-group">
                                            <label class="form-label">New Password</label>
                                            <input type="password" class="form-control" name="newPassword" id="newPassword" minlength="8" placeholder="Min 8 chars with requirements">
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="form-group">
                                            <label class="form-label">Confirm New Password</label>
                                            <input type="password" class="form-control" name="confirmNewPassword" id="confirmNewPassword" minlength="8" placeholder="Confirm password">
                                            <small class="text-success d-none" id="passwordMatchSuccess">
                                                <i class="fas fa-check-circle"></i> Passwords match
                                            </small>
                                            <small class="text-danger d-none" id="passwordMismatchError">
                                                <i class="fas fa-exclamation-circle"></i> Passwords do not match
                                            </small>
                                        </div>
                                    </div>
                                </div>

                                <!-- OTP Section (shown when password change is initiated) -->
                                <div class="otp-section" id="otpSection">
                                    <h6 style="margin-bottom: 12px; color: #856404;">
                                        <i class="fas fa-info-circle"></i> Email Verification Required
                                    </h6>
                                    <p style="margin: 10px 0; color: #856404; font-size: 13px;">
                                        An OTP has been sent to your email. Please enter it to confirm the password change for security purposes.
                                    </p>
                                    <div class="form-group" style="margin-top: 12px;">
                                        <label class="form-label" style="color: #856404;">Verification OTP</label>
                                        <input type="text" class="form-control" name="passwordOTP" id="passwordOTP" placeholder="Enter 6-digit OTP" maxlength="6" style="font-weight: 600; letter-spacing: 4px; text-align: center;">
                                        <small class="text-muted" id="otpTimer" style="margin-top: 5px; display: none;">OTP expires in: <span id="timerCount">10:00</span></small>
                                    </div>
                                </div>

                                <!-- Request OTP Button -->
                                <div style="margin-top: 15px;">
                                    <button type="button" class="btn btn-send-otp" id="sendOtpBtn" style="display: none;">
                                        <i class="fas fa-envelope"></i> Send Verification OTP to Email
                                    </button>
                                    <small class="text-muted d-block" style="margin-top: 8px;" id="otpHelpText">
                                        ℹ️ Enter your current password and new password, then click above to receive an OTP for verification.
                                    </small>
                                </div>
                            </div>

                            <div class="text-center" style="margin-top: 30px;">
                                <button type="submit" class="btn btn-edit" id="submitBtn">
                                    <i class="fas fa-save"></i> Update Profile
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script nonce="{{cspNonce}}">
        // Real-time Socket.IO support
        const socket = io();
        
        // Listen for profile update events from other sessions
        socket.on('profileUpdate', (data) => {
            if (data.message) {
                console.log('📡 Real-time notification:', data.message);
                // Could be used for cross-session updates in future
            }
        });

        // Password validation with real-time requirements checking
        const form = document.getElementById('profileForm');
        const newPasswordEl = document.getElementById('newPassword');
        const confirmNewPasswordEl = document.getElementById('confirmNewPassword');
        const currentPasswordEl = document.getElementById('currentPassword');
        const passwordError = document.getElementById('passwordError');
        const passwordErrorMsg = document.getElementById('passwordErrorMsg');
        const passwordRequirements = document.getElementById('passwordRequirements');
        const sendOtpBtn = document.getElementById('sendOtpBtn');
        const otpSection = document.getElementById('otpSection');
        const passwordOTPEl = document.getElementById('passwordOTP');
        const submitBtn = document.getElementById('submitBtn');
        
        // Password validation regex: 8+ chars, 1 uppercase, 1 lowercase, 1 number, 1 special char
        const passwordRule = /^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[^A-Za-z0-9]).{8,}$/;

        function hidePasswordError() {
            passwordError.classList.add('d-none');
            passwordErrorMsg.textContent = '';
        }

        function showPasswordError(msg) {
            passwordErrorMsg.textContent = msg;
            passwordError.classList.remove('d-none');
        }

        // Real-time password requirement checking
        newPasswordEl.addEventListener('input', function() {
            const pwd = this.value;
            
            if (pwd.length > 0) {
                passwordRequirements.classList.remove('d-none');
            } else {
                passwordRequirements.classList.add('d-none');
            }

            // Check each requirement
            const hasLength = pwd.length >= 8;
            const hasUppercase = /[A-Z]/.test(pwd);
            const hasLowercase = /[a-z]/.test(pwd);
            const hasNumber = /\d/.test(pwd);
            const hasSpecial = /[^A-Za-z0-9]/.test(pwd);

            // Update UI for each requirement
            updateRequirement('req-length', hasLength);
            updateRequirement('req-uppercase', hasUppercase);
            updateRequirement('req-lowercase', hasLowercase);
            updateRequirement('req-number', hasNumber);
            updateRequirement('req-special', hasSpecial);

            // Show/hide OTP button based on requirements
            const allMet = hasLength && hasUppercase && hasLowercase && hasNumber && hasSpecial;
            if (allMet && currentPasswordEl.value && confirmNewPasswordEl.value === pwd) {
                sendOtpBtn.style.display = 'inline-block';
            } else {
                sendOtpBtn.style.display = 'none';
            }
        });

        function updateRequirement(id, met) {
            const el = document.getElementById(id);
            if (met) {
                el.classList.remove('unmet');
                el.classList.add('met');
                el.querySelector('i').className = 'fas fa-check-circle';
            } else {
                el.classList.add('unmet');
                el.classList.remove('met');
                el.querySelector('i').className = 'fas fa-times-circle';
            }
        }

        // Real-time password match checking
        confirmNewPasswordEl.addEventListener('input', function() {
            const newPwd = newPasswordEl.value;
            const confirmPwd = this.value;

            if (confirmPwd && newPwd) {
                if (newPwd === confirmPwd) {
                    document.getElementById('passwordMatchSuccess').classList.remove('d-none');
                    document.getElementById('passwordMismatchError').classList.add('d-none');
                    // Show OTP button if all other conditions are met
                    if (passwordRule.test(newPwd) && currentPasswordEl.value) {
                        sendOtpBtn.style.display = 'inline-block';
                    }
                } else {
                    document.getElementById('passwordMatchSuccess').classList.add('d-none');
                    document.getElementById('passwordMismatchError').classList.remove('d-none');
                    sendOtpBtn.style.display = 'none';
                }
            } else {
                document.getElementById('passwordMatchSuccess').classList.add('d-none');
                document.getElementById('passwordMismatchError').classList.add('d-none');
            }
        });

        // Current password input listener
        currentPasswordEl.addEventListener('input', function() {
            // Show OTP button if all requirements are met
            const newPwd = newPasswordEl.value;
            const confirmPwd = confirmNewPasswordEl.value;
            if (passwordRule.test(newPwd) && confirmPwd === newPwd && this.value) {
                sendOtpBtn.style.display = 'inline-block';
            } else {
                sendOtpBtn.style.display = 'none';
            }
        });

        // Send OTP
        sendOtpBtn.addEventListener('click', async function() {
            const currentPassword = currentPasswordEl.value.trim();
            
            if (!currentPassword) {
                showPasswordError('Enter your current password');
                return;
            }

            sendOtpBtn.disabled = true;
            sendOtpBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending OTP...';

            try {
                const response = await fetch('/tenant/profile/send-password-otp', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ currentPassword })
                });

                const result = await response.json();

                if (result.success) {
                    // Show OTP section
                    otpSection.classList.add('show');
                    document.getElementById('otpTimer').style.display = 'block';
                    passwordOTPEl.focus();
                    
                    // Start OTP timer
                    startOTPTimer();
                    
                    hidePasswordError();
                    showPasswordError('');
                    alert('✅ OTP sent! Check your email and enter the code.');
                } else {
                    showPasswordError(result.message || 'Failed to send OTP');
                }
            } catch (error) {
                console.error('Error sending OTP:', error);
                showPasswordError('An error occurred. Please try again.');
            } finally {
                sendOtpBtn.disabled = false;
                sendOtpBtn.innerHTML = '<i class="fas fa-envelope"></i> Send Verification OTP to Email';
            }
        });

        // OTP Timer
        function startOTPTimer() {
            let timeRemaining = 600; // 10 minutes in seconds
            const timerCount = document.getElementById('timerCount');
            
            const timer = setInterval(() => {
                timeRemaining--;
                const minutes = Math.floor(timeRemaining / 60);
                const seconds = timeRemaining % 60;
                timerCount.textContent = `${minutes}:${seconds.toString().padStart(2, '0')}`;
                
                if (timeRemaining <= 0) {
                    clearInterval(timer);
                    otpSection.classList.remove('show');
                    document.getElementById('otpTimer').style.display = 'none';
                    passwordOTPEl.value = '';
                    showPasswordError('OTP has expired. Please click "Send Verification OTP" again.');
                }
            }, 1000);
        }

        // Form submission validation
        form.addEventListener('submit', function(e) {
            hidePasswordError();
            const newPassword = newPasswordEl.value.trim();
            const confirmNewPassword = confirmNewPasswordEl.value.trim();
            const currentPassword = currentPasswordEl.value.trim();
            const passwordOTP = passwordOTPEl.value.trim();

            // If any password field has content, validate all password changes
            if (newPassword || confirmNewPassword || currentPassword) {
                if (!currentPassword) {
                    e.preventDefault();
                    showPasswordError('Enter your current password to change it');
                    currentPasswordEl.focus();
                    return;
                }

                if (!newPassword || !confirmNewPassword) {
                    e.preventDefault();
                    showPasswordError('Please fill in both new password fields');
                    newPasswordEl.focus();
                    return;
                }

                if (!passwordRule.test(newPassword)) {
                    e.preventDefault();
                    showPasswordError('Password must be 8+ characters with 1 uppercase, 1 lowercase, 1 number, and 1 special character');
                    newPasswordEl.focus();
                    return;
                }

                if (newPassword !== confirmNewPassword) {
                    e.preventDefault();
                    showPasswordError('New passwords do not match');
                    confirmNewPasswordEl.focus();
                    return;
                }

                // Check if OTP is required
                if (otpSection.classList.contains('show')) {
                    if (!passwordOTP) {
                        e.preventDefault();
                        showPasswordError('Please enter the OTP sent to your email');
                        passwordOTPEl.focus();
                        return;
                    }
                    if (!/^\d{6}$/.test(passwordOTP)) {
                        e.preventDefault();
                        showPasswordError('OTP must be a 6-digit number');
                        passwordOTPEl.focus();
                        return;
                    }
                }
            }
        });

        // Auto-dismiss alerts after 5 seconds
        setTimeout(function() {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                const bsAlert = new bootstrap.Alert(alert);
                bsAlert.close();
            });
        }, 5000);

        // Socket.IO - Emit profile update event
        socket.on('connect', () => {
            console.log('✅ Real-time connection established');
        });

        socket.on('disconnect', () => {
            console.log('⚠️ Real-time connection lost');
        });
    </script>
</body>
</html>
